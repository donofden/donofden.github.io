source 'https://rubygems.org'

# Pin to the version GitHub Pages currently uses.
# Check https://pages.github.com/versions/ to see if this needs updating.
gem 'github-pages', '232', group: :jekyll_plugins

# Explicitly include plugins used in _config.yml
gem 'jekyll-github-metadata', '2.16.1'
gem 'jekyll-octicons', '0.8.0'
gem 'jemoji', '0.13.0'

# Required for Ruby 3.x compatibility with Jekyll 3.x
gem 'webrick', '~> 1.8'
